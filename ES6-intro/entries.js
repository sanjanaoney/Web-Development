const employee={
    name: 'bob',
    designation: 'qa',
    salary:20000,
    experience: 1
}

const keys=Object.keys(employee)
const values=Object.values(employee)
const entries=Object.entries(employee)
console.log(keys)
console.log(values)
//if we want to get keys and values both
console.log(entries)